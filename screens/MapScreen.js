import React, { Component } from 'react';
import { Text, View } from 'react-native';

const MapScreen= () => {
    return(
        <view>
          <text>Hey</text>  
        </view>
    );
};

export default MapScreen;
